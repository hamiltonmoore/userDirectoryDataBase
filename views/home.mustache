<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport">
  <link rel="stylesheet" href="/css/main.css">
  <title>user directory</title>
</head>

<body>

  <div class="wrapper">
    <h1>index</h1>
    <div class="container">
      {{#users}}
      <div class="box">
        <div>{{name}}</div>
        <img src="{{avatar}}" alt="#">
        <div>{{job}}</div>
        <div class="hire">{{^job}}for hire{{/job}}</div>
        <form action="/profile/{{id}}" method="GET">
          <button type="submit">See Profile</button>
        </form>
      </div>
      {{/users}}
    </div>
  </div>
</body>

</html>